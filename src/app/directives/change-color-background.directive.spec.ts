import { ChangeColorBackgroundDirective } from './change-color-background.directive';

describe('ChangeColorBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeColorBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
